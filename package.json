{
  "name": "cc-limit-resume",
  "version": "0.1.0",
  "description": "Explicit one-shot Claude Code rate-limit resume helper",
  "type": "module",
  "bin": {
    "cc-limit-resume": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "scripts",
    "claude-plugin",
    "codex-plugin"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepack": "pnpm build",
    "prepublishOnly": "pnpm test"
  },
  "keywords": [
    "claude-code",
    "rate-limit",
    "resume"
  ],
  "license": "MIT",
  "devDependencies": {
    "tsdown": "^0.22.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.5"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./dist/cli.mjs",
    "./package.json": "./package.json"
  }
}
